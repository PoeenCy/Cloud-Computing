# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - Keycloak Hardcoded Password & Flask Unhandled Exception
  - **CRITICAL**: This test MUST FAIL on unfixed code — failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior — it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate both bugs exist
  - **Scoped PBT Approach**: Scope to concrete failing cases for reproducibility
  - **Sub-test A — Compose Config (Bug 1)**:
    - Parse `MiniCloud/docker-compose.yml` và assert `auth.environment` KHÔNG chứa key `KC_DB_PASSWORD` hoặc `KEYCLOAK_ADMIN_PASSWORD`
    - Chạy trên code CHƯA fix → test FAIL vì cả hai key hardcoded vẫn còn
    - Counterexample: `KC_DB_PASSWORD: "secure_db_password_123!"` tồn tại trong compose
  - **Sub-test B — Flask Secret Missing (Bug 2)**:
    - Gọi `get_db_connection()` khi `/run/secrets/db_password` không tồn tại (mock `os.path.exists` trả về False)
    - Assert raises `ConnectionError` (không phải `RuntimeError`, không crash process)
    - Chạy trên code CHƯA fix → test FAIL vì `RuntimeError` được raise thay vì `ConnectionError`
    - Counterexample: `get_db_connection()` raises `RuntimeError` thay vì `ConnectionError`
  - **Sub-test C — Route DB Failure (Bug 2)**:
    - Gọi `GET /api/student` khi `get_db_connection()` raise exception (mock)
    - Assert HTTP response status = 503
    - Chạy trên code CHƯA fix → test FAIL vì route chưa có try/except, trả về HTTP 500 hoặc crash
    - Counterexample: route trả về HTTP 500 thay vì 503
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests FAIL (this is correct — it proves the bugs exist)
  - Document counterexamples found to understand root cause
  - Mark task complete when tests are written, run, and failures are documented
  - _Requirements: 1.1, 1.5, 1.6_

- [ ] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Non-Buggy Inputs Unchanged
  - **IMPORTANT**: Follow observation-first methodology
  - **Observe on UNFIXED code** (non-buggy inputs — cases where isBugCondition returns false):
    - Observe: `GET /api/hello` trả về HTTP 200 với `{"message": "Hello from Modular App Server!"}`
    - Observe: `get_db_connection()` với mock secret file hợp lệ không ném exception (chỉ fail ở bước connect MariaDB nếu DB không có)
    - Observe: Các service khác trong `docker-compose.yml` (db, web, proxy, storage, monitoring) không thay đổi cấu hình
    - Observe: `from src import create_app` không có side effect ở module-level
  - **Write property-based tests capturing observed behavior**:
    - Property A: Với mọi HTTP request đến `/api/hello`, response SHALL luôn là HTTP 200 với đúng payload (bất kể trạng thái DB)
    - Property B: Với mọi nội dung secret file hợp lệ (non-empty string), `_read_db_password_secret()` SHALL trả về giá trị đã strip, không ném exception
    - Property C: Cấu hình các service không liên quan (db, web, proxy, storage, monitoring, loki, grafana, dns) trong `docker-compose.yml` SHALL không thay đổi sau fix
    - Property D: Module `src` SHALL import được mà không có side effect ở module-level (không gọi `get_db_connection()` khi import)
  - Verify tests PASS on UNFIXED code (confirms baseline behavior to preserve)
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [ ] 3. Fix for Keycloak password mismatch and Flask startup crash

  - [x] 3.1 Fix `MiniCloud/docker-compose.yml` — service `auth`
    - Xóa dòng `KC_DB_PASSWORD: "secure_db_password_123!"` khỏi `environment` block của service `auth`
    - Thêm `KC_DB_PASSWORD_FILE: /run/secrets/db_password` vào `environment` block
    - Xóa dòng `KEYCLOAK_ADMIN_PASSWORD: "keycloak_admin_super_secret_123!"` khỏi `environment` block
    - Thêm `KEYCLOAK_ADMIN_PASSWORD_FILE: /run/secrets/kc_admin_password` vào `environment` block
    - Giữ nguyên toàn bộ cấu hình network, secrets mount, healthcheck, depends_on, command
    - _Bug_Condition: isBugCondition_KC(config) — `KC_DB_PASSWORD` hoặc `KEYCLOAK_ADMIN_PASSWORD` là hardcoded string trong `auth.environment`_
    - _Expected_Behavior: `auth.environment` chứa `KC_DB_PASSWORD_FILE: /run/secrets/db_password` và `KEYCLOAK_ADMIN_PASSWORD_FILE: /run/secrets/kc_admin_password`; Keycloak kết nối MariaDB thành công_
    - _Preservation: Các service khác (db, web, proxy, storage, monitoring) không thay đổi; secrets mount giữ nguyên_
    - _Requirements: 2.1, 2.3, 3.3, 3.4_

  - [x] 3.2 Fix `MiniCloud/app/src/database.py` — hàm `get_db_connection()`
    - Wrap toàn bộ body của `get_db_connection()` trong try/except
    - Bắt `RuntimeError` từ `_read_db_password_secret()` → re-raise dưới dạng `ConnectionError` với message rõ ràng
    - Bắt `mysql.connector.Error` cho các lỗi kết nối MariaDB → raise `ConnectionError` với message rõ ràng
    - Không thay đổi `_read_db_password_secret()` (hàm này đã đúng — lazy, không gọi ở module-level)
    - _Bug_Condition: isBugCondition_Flask(env) — `get_db_connection()` ném `RuntimeError` không được bắt khi secret file không tồn tại_
    - _Expected_Behavior: `get_db_connection()` ném `ConnectionError` (không phải `RuntimeError`) khi secret missing hoặc DB unreachable; Flask process không crash_
    - _Preservation: Khi secret file tồn tại và DB healthy, `get_db_connection()` hoạt động bình thường như trước_
    - _Requirements: 2.6, 3.2, 3.5_

  - [x] 3.3 Fix `MiniCloud/app/src/routes.py` — route `/api/student`
    - Thêm try/except bao quanh `get_db_connection()` trong hàm `get_students()`
    - Bắt `ConnectionError` → trả về `jsonify({"error": "Service Unavailable"}), 503`
    - Implement phần TODO: gọi `get_db_connection()`, execute query, trả về danh sách sinh viên
    - _Bug_Condition: isBugCondition_Flask(env) — `get_db_connection()` được gọi từ route mà không có try/except_
    - _Expected_Behavior: Route trả về HTTP 503 khi `ConnectionError`; trả về HTTP 200 với data khi DB healthy_
    - _Preservation: Route `/api/hello` không thay đổi; không có side effect ở module-level_
    - _Requirements: 2.7, 3.1, 3.2_

  - [ ] 3.4 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - Keycloak Reads from Secret File & Flask Returns 503
    - **IMPORTANT**: Re-run the SAME tests from task 1 — do NOT write new tests
    - The tests from task 1 encode the expected behavior
    - When these tests pass, it confirms the expected behavior is satisfied
    - Run all three sub-tests (A, B, C) from task 1 on FIXED code
    - **EXPECTED OUTCOME**: All tests PASS (confirms both bugs are fixed)
    - Sub-test A: `docker-compose.yml` không còn `KC_DB_PASSWORD` hardcoded → PASS
    - Sub-test B: `get_db_connection()` raises `ConnectionError` khi secret missing → PASS
    - Sub-test C: Route `/api/student` trả về HTTP 503 khi DB fail → PASS
    - _Requirements: 2.1, 2.6, 2.7_

  - [ ] 3.5 Verify preservation tests still pass
    - **Property 2: Preservation** - Non-Buggy Inputs Unchanged
    - **IMPORTANT**: Re-run the SAME tests from task 2 — do NOT write new tests
    - Run all preservation property tests (A, B, C, D) from task 2 on FIXED code
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm `/api/hello` vẫn trả về HTTP 200 với đúng payload
    - Confirm `_read_db_password_secret()` vẫn hoạt động đúng với secret file hợp lệ
    - Confirm cấu hình các service không liên quan không thay đổi
    - Confirm module import không có side effect

- [ ] 4. Checkpoint — Ensure all tests pass
  - Chạy toàn bộ test suite, đảm bảo tất cả pass
  - Verify `docker-compose.yml`: service `auth` dùng `KC_DB_PASSWORD_FILE` và `KEYCLOAK_ADMIN_PASSWORD_FILE`, không còn hardcoded password
  - Verify `database.py`: `get_db_connection()` có try/except, raise `ConnectionError`
  - Verify `routes.py`: route `/api/student` có try/except, trả về HTTP 503 khi `ConnectionError`
  - Nếu có câu hỏi phát sinh trong quá trình implement, hỏi user trước khi tiếp tục
  - Ensure all tests pass, ask the user if questions arise.

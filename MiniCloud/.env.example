# Database Configuration
DB_NAME=minicloud
DB_USER=admin

# Notice: All passwords are managed via Docker Secrets and stored in the ./secrets/ directory.
# This prevents sensitive credentials from lingering in environment variables where they could be exposed.
